enum RangeSelector { DAILY, WEEKLY, MONTHLY }

enum TelemetryStatus { ACTIVE, INACTIVE, UNAVAILABLE }

enum CType { IN, OUT, UNDEFINED }

enum CurrentlyShowing { MACHINES, PRIZES, SUPPLIES }

enum Period { DAILY, WEEKLY, MONTHLY }
